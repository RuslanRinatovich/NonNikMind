<Page x:Class="MindKeeper.Pages.TagsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Управление тегами">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель добавления тега -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox x:Name="TxtNewTag" 
                     materialDesign:HintAssist.Hint="Новый тег"
                     Text="{Binding NewTagName, UpdateSourceTrigger=PropertyChanged}"
                     Width="200" Margin="0,0,10,0"/>
            <Button Content="➕ Добавить" Command="{Binding AddTagCommand}" 
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="{DynamicResource PrimaryHueMidBrush}"/>
        </StackPanel>

        <!-- Информационное сообщение -->
        <TextBlock Grid.Row="1" 
                   Text="ℹ️ Вы можете удалять любые теги. Тег будет удалён из всех заметок."
                   FontSize="11" 
                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                   Margin="0,5,0,10"/>

        <!-- Таблица тегов -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Tags}"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название тега" Binding="{Binding TagName}" Width="*"/>
                <DataGridTextColumn Header="Мои заметки" Binding="{Binding UserNoteCount}" Width="80"/>
                <DataGridTextColumn Header="Всего заметок" Binding="{Binding GlobalNoteCount}" Width="80"/>
                <DataGridTemplateColumn Header="Действия" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="🗑" 
                                    Command="{Binding DataContext.DeleteTagCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource MaterialDesignToolButton}"
                                    ToolTip="Удалить тег"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>